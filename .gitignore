# Dependencies
node_modules/

# Next.js
.next/
out/

# Environment
.env
.env*.local
.env.production
.env.development

# OS
.DS_Store

# MCP config (contains auth tokens)
.mcp.json

# Documents & Legal (not code)
#
# The markdown docs ARE committed. privacy-accuracy.test.ts reads the Systems
# Guide to prove it no longer claims PostHog is anonymous, and a guard that
# reads a file only present on one person's Mac passes there and fails in CI,
# which is exactly how it sat broken for six weeks (#499). A doc the product
# makes a legal claim about belongs in git with the code it describes.
#
# The binaries (the brand kit PDF, the PRD .docx) stay out: they diff badly
# and nothing tests them.
Documents/*
!Documents/*.md

# The PRD stays out, for now and for a stated reason rather than by oversight.
# It was written in April with em dashes in it, and the pre-push style gate
# refuses them. Bringing it in means editing the punctuation of a record
# somebody wrote, which is a different act from moving that record into git.
# #496 already needs it edited (it states a family price the product does not
# charge); it can come in on that change, rewritten deliberately.
Documents/NurseDex_PRD_v2.md

# Anchored with a leading slash on purpose. Unanchored, "Legal/" matches a
# directory of that name at EVERY depth, and it silently swallowed
# src/lib/legal/ (a gitignored source file is invisible: status stays clean,
# the commit looks right, and CI fails on an import of something never pushed).
# src/source-tracked.test.ts fails if anything under src/ is ignored again.
/Legal/

# Claude
.claude/

# IDE
.vscode/
.idea/

# Testing
/test-results/
/playwright-report/
/blob-report/
/coverage/
/e2e/.auth/

# Playwright MCP browser snapshots (generated, local-only)
/.playwright-mcp/

# Sentry
.sentryclirc

# Misc
*.tsbuildinfo
next-env.d.ts
.vercel

# Vercel plugin agent guidance (generated, local-only)
AGENTS.md

# Skill manager lockfile (generated, local-only)
skills-lock.json

# Vitest's transform and results caches. Set outside node_modules in #811 so
# they survive npm ci; never committed.
.vitest-cache/
